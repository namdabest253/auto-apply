---
phase: 03.1-internship-discovery-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/scrapers/filters.ts
  - src/lib/scrapers/filters.test.ts
  - src/lib/scrapers/constants.ts
  - src/lib/scrapers/constants.test.ts
  - src/lib/scrapers/greenhouse.ts
  - src/lib/scrapers/greenhouse.test.ts
autonomous: true
requirements: [INTERN-01, INTERN-02, INTERN-03]

must_haves:
  truths:
    - "Internship filtering correctly identifies intern/internship/co-op roles via word-boundary regex"
    - "Negative keywords (senior, staff, principal, lead, manager, director, phd, mba, vp, head of, fellow) exclude non-intern roles"
    - "Greenhouse scraper only returns internship-level roles after filtering"
    - "Combined Greenhouse company list has 50+ entries with verified slugs"
  artifacts:
    - path: "src/lib/scrapers/filters.ts"
      provides: "Shared internship filtering utilities"
      exports: ["isInternshipRole", "filterInternships"]
    - path: "src/lib/scrapers/filters.test.ts"
      provides: "Unit tests for positive/negative internship filtering"
      min_lines: 40
    - path: "src/lib/scrapers/constants.ts"
      provides: "Greenhouse (50+) and Lever (18) company lists"
      contains: "LEVER_COMPANIES"
    - path: "src/lib/scrapers/constants.test.ts"
      provides: "Company list count verification"
      min_lines: 10
  key_links:
    - from: "src/lib/scrapers/greenhouse.ts"
      to: "src/lib/scrapers/filters.ts"
      via: "import { isInternshipRole } from './filters'"
      pattern: "isInternshipRole"
    - from: "src/lib/scrapers/greenhouse.ts"
      to: "src/lib/scrapers/constants.ts"
      via: "import { GREENHOUSE_COMPANIES }"
      pattern: "GREENHOUSE_COMPANIES"
---

<objective>
Create shared internship filtering utilities, expand the company list to 50+ Greenhouse companies plus 18 Lever companies, and update the Greenhouse scraper to filter for intern-level roles only.

Purpose: The current Greenhouse scraper returns all jobs (~5800) with only ~47 being internships. This plan adds positive/negative keyword filtering and expands the company pool to dramatically increase relevant internship yield.
Output: filters.ts (shared filtering), expanded constants.ts, updated greenhouse.ts with filtering applied
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-internship-discovery-improvements/03.1-RESEARCH.md

@src/lib/scrapers/types.ts
@src/lib/scrapers/greenhouse.ts
@src/lib/scrapers/constants.ts
@src/lib/scrapers/stealth.ts

<interfaces>
<!-- Key types the executor needs -->

From src/lib/scrapers/types.ts:
```typescript
export interface ScraperAdapter {
  platform: string;
  discover(params: SearchParams): Promise<DiscoveredJob[]>;
}

export interface SearchParams {
  keywords: string[];
  locations: string[];
  roleTypes: string[];
}

export interface DiscoveredJob {
  externalUrl: string;
  platform: string;
  title: string;
  company: string;
  location: string | null;
  datePosted: Date | null;
  descriptionHtml: string | null;
  descriptionText: string | null;
  salary: string | null;
  metadata: Record<string, unknown>;
}
```

From src/lib/scrapers/constants.ts:
```typescript
export interface GreenhouseCompany {
  slug: string;
  name: string;
}
// Currently 23 companies
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared internship filtering module with tests</name>
  <files>src/lib/scrapers/filters.ts, src/lib/scrapers/filters.test.ts</files>
  <behavior>
    - Positive matching: "Software Engineering Intern" -> true
    - Positive matching: "Internship - Data Science" -> true
    - Positive matching: "Summer Co-op Engineer" -> true
    - Positive matching: "co-op" (no hyphen) -> true
    - Positive matching with department: generic title + department "Internship Program" -> true
    - Negative rejection: "Internal Tools Engineer" -> false (no word boundary match)
    - Negative rejection: "International Sales" -> false
    - Negative exclusion: "Senior Software Engineer Intern" -> false (negative keyword present)
    - Negative exclusion: "PhD Research Intern" -> false
    - Negative exclusion: "MBA Intern" -> false
    - Negative exclusion: "Staff Engineer" -> false
    - Negative exclusion: "Principal Engineer" -> false
    - Negative exclusion: "Lead Developer" -> false
    - Negative exclusion: "Director of Engineering" -> false
    - Negative exclusion: "VP of Product" -> false
    - Negative exclusion: "Head of Design" -> false
    - Negative exclusion: "Fellow" -> false
    - No match: "Software Engineer" (no intern keyword) -> false
    - filterInternships: filters array correctly using title and optional department getter
  </behavior>
  <action>
    1. Write tests FIRST in `filters.test.ts` covering all behaviors above. Tests should import `isInternshipRole` and `filterInternships` from `./filters`. Run tests -- they MUST fail (RED).

    2. Create `src/lib/scrapers/filters.ts` exporting:
       - `INTERN_POSITIVE_PATTERNS`: array of RegExp with word boundaries for "intern", "internship", "co-op" (with optional hyphen)
       - `INTERN_NEGATIVE_PATTERNS`: array of RegExp with word boundaries for senior, staff, principal, lead, manager, director, phd, mba, vp, "head of", fellow
       - `isInternshipRole(title: string, department?: string): boolean` -- concatenates title + department, checks positive match on combined text, checks negative match on title only, returns true only if positive match AND no negative match
       - `filterInternships<T extends { title: string }>(jobs: T[], getDepartment?: (job: T) => string | undefined): T[]` -- filters array using isInternshipRole

    3. Run tests -- they MUST pass (GREEN).
  </action>
  <verify>
    <automated>npx vitest run src/lib/scrapers/filters.test.ts</automated>
  </verify>
  <done>All positive/negative filtering tests pass. isInternshipRole and filterInternships exported and working.</done>
</task>

<task type="auto">
  <name>Task 2: Expand company lists and update Greenhouse scraper with filtering</name>
  <files>src/lib/scrapers/constants.ts, src/lib/scrapers/constants.test.ts, src/lib/scrapers/greenhouse.ts, src/lib/scrapers/greenhouse.test.ts</files>
  <action>
    1. Update `src/lib/scrapers/constants.ts`:
       - Keep existing `GreenhouseCompany` interface and 23 companies
       - Add these verified Greenhouse companies (slug -> name): spacex/SpaceX, rubrik/Rubrik, roku/Roku, purestorage/Pure Storage, togetherai/Together AI, sigmacomputing/Sigma Computing, affirm/Affirm, gleanwork/Glean, anthropic/Anthropic, pinterest/Pinterest, lyft/Lyft, instacart/Instacart, dropbox/Dropbox, robinhood/Robinhood, chime/Chime, datadog/Datadog, mongodb/MongoDB, reddit/Reddit, grammarly/Grammarly, elastic/Elastic, samsara/Samsara, nuro/Nuro, waymo/Waymo, lucidmotors/Lucid Motors, toast/Toast, postman/Postman, cockroachlabs/Cockroach Labs, temporaltechnologies/Temporal, asana/Asana, amplitude/Amplitude, unity3d/Unity, epicgames/Epic Games, riotgames/Riot Games, twitch/Twitch, block/Block, pagerduty/PagerDuty, aquaticcapitalmanagement/Aquatic Capital
       - Add `LeverCompany` interface (same shape: { slug: string; name: string })
       - Add `LEVER_COMPANIES: LeverCompany[]` with these verified slugs: palantir/Palantir, spotify/Spotify, atlassian/Atlassian, plaid/Plaid, brilliant/Brilliant, wealthsimple/Wealthsimple, voleon/The Voleon Group, anyscale/Anyscale, stackadapt/StackAdapt, zoox/Zoox, color/Color Health, lever/Lever, theathletic/The Athletic, saviynt/Saviynt, pyka/Pyka, weride/WeRide, sep/SEP, solopulseco/SoloPulse

    2. Create `src/lib/scrapers/constants.test.ts`:
       - Test that GREENHOUSE_COMPANIES.length >= 50
       - Test that LEVER_COMPANIES.length >= 15
       - Test that all entries have non-empty slug and name
       - Test no duplicate slugs within each list

    3. Update `src/lib/scrapers/greenhouse.ts`:
       - Add import: `import { isInternshipRole } from "./filters"`
       - Replace the existing keyword filtering block (lines 59-66 in current file) with internship filtering:
         ```typescript
         // Filter for internship roles
         const filtered = jobs.filter((j) =>
           isInternshipRole(
             j.title,
             ((j.metadata as any)?.departments || [])
               .map((d: any) => d.name)
               .join(" ")
           )
         );
         ```
       - Remove the `params.keywords` filtering entirely -- internship filtering replaces it

    4. Update `src/lib/scrapers/greenhouse.test.ts`:
       - Add tests verifying that the Greenhouse scraper now filters for internships (mock fetch to return mix of intern and non-intern jobs, verify only intern jobs returned)

    5. Run all scraper tests to verify nothing is broken.
  </action>
  <verify>
    <automated>npx vitest run src/lib/scrapers/</automated>
  </verify>
  <done>Greenhouse company list has 50+ entries. Lever company list has 18 entries. Greenhouse scraper applies internship filtering via shared filters.ts. All scraper tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/scrapers/` -- all scraper tests pass
- `grep -c "slug:" src/lib/scrapers/constants.ts` confirms 50+ Greenhouse + 18 Lever entries
- `grep "isInternshipRole" src/lib/scrapers/greenhouse.ts` confirms filtering is wired
</verification>

<success_criteria>
- filters.ts exports isInternshipRole and filterInternships with correct positive/negative pattern matching
- constants.ts has 50+ Greenhouse companies and 18 Lever companies
- Greenhouse scraper uses isInternshipRole to filter results (no more keyword-only filtering)
- All tests pass: filters.test.ts, constants.test.ts, greenhouse.test.ts
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-internship-discovery-improvements/03.1-01-SUMMARY.md`
</output>
