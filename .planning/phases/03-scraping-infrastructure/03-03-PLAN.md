---
phase: 03-scraping-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/app/(dashboard)/page.tsx
  - src/app/(dashboard)/actions.ts
  - src/app/(dashboard)/components/jobs-table.tsx
  - src/app/(dashboard)/components/jobs-columns.tsx
  - src/app/(dashboard)/components/job-detail-panel.tsx
  - src/app/(dashboard)/components/scrape-status-bar.tsx
  - src/components/nav/top-nav.tsx
  - src/components/ui/table.tsx
  - src/components/ui/sheet.tsx
autonomous: false
requirements:
  - DISC-01
  - DISC-02

must_haves:
  truths:
    - "User sees a jobs table as the default landing page after login"
    - "User can click Discover Jobs button to trigger scraping of all platforms"
    - "Status bar shows scrape progress: idle, running, completed (X new jobs), or failed"
    - "Jobs table shows title, company, location, date posted in sortable columns"
    - "Clicking a row opens a side panel with full job details and link to original posting"
    - "Stale jobs (30+ days old) have a visual indicator"
    - "Table refreshes after scrape completion to show new jobs"
  artifacts:
    - path: "src/app/(dashboard)/page.tsx"
      provides: "Jobs page (replaces placeholder dashboard)"
      min_lines: 20
    - path: "src/app/(dashboard)/components/jobs-table.tsx"
      provides: "DataTable with TanStack Table for job listings"
      min_lines: 50
    - path: "src/app/(dashboard)/components/job-detail-panel.tsx"
      provides: "Sheet slide-out panel for job details"
      min_lines: 30
    - path: "src/app/(dashboard)/components/scrape-status-bar.tsx"
      provides: "Status banner showing scrape progress"
      min_lines: 20
    - path: "src/app/(dashboard)/actions.ts"
      provides: "Server actions for triggering scrape and fetching jobs"
      exports: ["triggerScrape", "getJobs"]
  key_links:
    - from: "src/app/(dashboard)/components/scrape-status-bar.tsx"
      to: "/api/scrape/status"
      via: "polling with useEffect/setInterval"
      pattern: "fetch.*api/scrape/status"
    - from: "src/app/(dashboard)/actions.ts"
      to: "/api/scrape/trigger"
      via: "server action calls trigger endpoint or queue directly"
      pattern: "scrapeQueue|api/scrape/trigger"
    - from: "src/app/(dashboard)/components/jobs-table.tsx"
      to: "src/app/(dashboard)/actions.ts"
      via: "fetches job data"
      pattern: "getJobs|fetchJobs"
---

<objective>
Build the jobs dashboard UI: data table with sortable columns, side panel for job details, status bar for scrape progress, and the Discover Jobs trigger button. This replaces the placeholder dashboard page and becomes the default landing after login.

Purpose: Gives the user visibility into discovered jobs and control over the scraping process.
Output: Complete jobs page with table, detail panel, status bar, and scrape trigger -- wired to the API routes and queue from Plan 02.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scraping-infrastructure/03-CONTEXT.md
@.planning/phases/03-scraping-infrastructure/03-RESEARCH.md
@.planning/phases/03-scraping-infrastructure/03-01-SUMMARY.md
@.planning/phases/03-scraping-infrastructure/03-02-SUMMARY.md

@src/app/(dashboard)/layout.tsx
@src/components/nav/top-nav.tsx

<interfaces>
<!-- From Plan 01 -->
From prisma/schema.prisma (Plan 01):
```prisma
model JobListing {
  id              String    @id @default(cuid())
  userId          String
  externalUrl     String
  platform        String
  title           String
  company         String
  location        String?
  datePosted      DateTime?
  descriptionHtml String?   @db.Text
  descriptionText String?   @db.Text
  salary          String?
  metadata        Json?
  isStale         Boolean   @default(false)
  scrapeRunId     String?
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
  @@unique([userId, externalUrl])
}

model ScrapeRun {
  id          String       @id @default(cuid())
  userId      String
  status      String       // "running" | "completed" | "failed"
  platforms   String[]
  jobsFound   Int          @default(0)
  errors      Json?
  startedAt   DateTime     @default(now())
  completedAt DateTime?
  duration    Int?
}
```

<!-- From Plan 02 -->
From src/app/api/scrape/trigger/route.ts (Plan 02):
```typescript
// POST /api/scrape/trigger
// Returns: { runId: string }
```

From src/app/api/scrape/status/route.ts (Plan 02):
```typescript
// GET /api/scrape/status?runId={id}
// Returns: { id, status, platforms, jobsFound, errors, startedAt, completedAt, duration }
```

From src/workers/queue.ts (Plan 02):
```typescript
export const scrapeQueue: Queue;
```

<!-- Existing -->
From src/components/nav/top-nav.tsx:
```typescript
// Client component with useSession, nav links, logout button
// Needs "Jobs" link added (or keep "/" as jobs page)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install UI components + server actions + jobs page shell</name>
  <files>src/components/ui/table.tsx, src/components/ui/sheet.tsx, src/app/(dashboard)/actions.ts, src/app/(dashboard)/page.tsx, src/components/nav/top-nav.tsx</files>
  <action>
1. Install shadcn table and sheet components: `npx shadcn@latest add table sheet --yes`
2. Install TanStack React Table: `npm install @tanstack/react-table`

3. Create src/app/(dashboard)/actions.ts with server actions:
   - `triggerScrape()`: auth check via `auth()`. Get user's JobPreference (via profile). If no preferences, return `{ error: "Set job preferences first" }`. Create ScrapeRun record (status "pending", platforms ["indeed", "greenhouse"]). Add job to scrapeQueue. Return `{ runId }`.
   - `getJobs(options?: { includeStale?: boolean })`: auth check. Fetch JobListings for user, ordered by createdAt desc. If includeStale is false (default), filter where isStale is false. Return array of jobs.
   - `getLatestScrapeRun()`: auth check. Return most recent ScrapeRun for user.

4. Rewrite src/app/(dashboard)/page.tsx:
   - Server component that fetches jobs via `getJobs()` and latest scrape run via `getLatestScrapeRun()`.
   - Renders: ScrapeStatusBar, Discover Jobs button, JobsTable with data.
   - Page title: "Jobs" with count badge.
   - "Discover Jobs" button uses client component wrapper to call triggerScrape action.
   - Pass jobs data to JobsTable, scrapeRun to ScrapeStatusBar.

5. Update src/components/nav/top-nav.tsx:
   - Add "Jobs" link pointing to "/" (since jobs page IS the home page now).
   - Keep existing "Profile" link.
   - Active state highlighting on current page.
  </action>
  <verify>
    <automated>cd /root/job && npx tsc --noEmit --pretty 2>&1 | head -20 && test -f src/components/ui/table.tsx && test -f src/components/ui/sheet.tsx && echo "UI components installed" && test -f src/app/\(dashboard\)/actions.ts && echo "actions exist"</automated>
  </verify>
  <done>shadcn table and sheet installed. Server actions for triggerScrape, getJobs, getLatestScrapeRun created. Jobs page shell replaces placeholder. Nav updated with Jobs link.</done>
</task>

<task type="auto">
  <name>Task 2: Jobs data table + columns + detail panel + status bar</name>
  <files>src/app/(dashboard)/components/jobs-table.tsx, src/app/(dashboard)/components/jobs-columns.tsx, src/app/(dashboard)/components/job-detail-panel.tsx, src/app/(dashboard)/components/scrape-status-bar.tsx</files>
  <action>
1. Create src/app/(dashboard)/components/jobs-columns.tsx:
   - Define TanStack Table column definitions for JobListing.
   - Columns: Title (main, wider), Company, Location (show "Not specified" if null), Date Posted (formatted relative like "2 days ago"), Platform (badge: Indeed/Greenhouse).
   - Stale indicator: if job is stale (isStale or datePosted > 30 days ago), show a subtle visual indicator (dimmed row or small "stale" badge).
   - Default sort by createdAt descending (newest first).

2. Create src/app/(dashboard)/components/jobs-table.tsx:
   - Client component ("use client").
   - Uses @tanstack/react-table with useReactTable, getCoreRowModel, getSortedRowModel, getPaginationRowModel.
   - Renders shadcn Table component with column headers (clickable to sort) and rows.
   - Clicking a row sets selectedJobId state and opens the detail panel.
   - Pagination controls at bottom (Previous/Next with page count).
   - Empty state: "No jobs discovered yet. Click Discover Jobs to get started."
   - Filter toggle: "Show stale jobs" checkbox.
   - Dark mode styling consistent with zinc palette.

3. Create src/app/(dashboard)/components/job-detail-panel.tsx:
   - Client component using shadcn Sheet (side="right", width ~40% of viewport).
   - Receives selected JobListing data.
   - Shows: title (large), company, location, date posted, platform, salary (if available).
   - Job description: render descriptionHtml safely (use dangerouslySetInnerHTML with basic sanitization, or render descriptionText as fallback).
   - "View Original" link button opening externalUrl in new tab.
   - Close button to dismiss panel.

4. Create src/app/(dashboard)/components/scrape-status-bar.tsx:
   - Client component.
   - Receives initial scrapeRun data from server component.
   - When a scrape is running (status !== "completed" and !== "failed"), polls GET /api/scrape/status?runId={id} every 2 seconds.
   - Displays contextual banner:
     - Idle (no active run): hidden
     - Running: blue/indigo banner "Discovering jobs from Indeed, Greenhouse..." with spinner
     - Completed: green banner "Found X new jobs" -- auto-dismisses after 5 seconds
     - Failed: red banner "Scraping failed" with error details -- stays until dismissed
   - On completion, calls router.refresh() to reload the server component data (per RESEARCH.md pitfall 5 -- stale data fix).
  </action>
  <verify>
    <automated>cd /root/job && npx tsc --noEmit --pretty 2>&1 | head -20 && test -f src/app/\(dashboard\)/components/jobs-table.tsx && test -f src/app/\(dashboard\)/components/job-detail-panel.tsx && test -f src/app/\(dashboard\)/components/scrape-status-bar.tsx && echo "All UI components created"</automated>
  </verify>
  <done>Jobs table renders with sortable columns, pagination, stale indicators. Side panel shows full job details with link to original. Status bar polls scrape progress and auto-refreshes table on completion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete jobs discovery flow end-to-end</name>
  <files>n/a</files>
  <action>
Human verifies the complete jobs discovery flow end-to-end.

What was built: Complete jobs discovery UI with data table (sortable columns: title, company, location, date, platform), slide-out detail panel, scrape status bar, and Discover Jobs button. Jobs page is the default landing after login. BullMQ worker processes scrape jobs in the background.

Verification steps:
1. Start the app: `docker compose up` (or `npm run dev` + `npm run worker:scrape`)
2. Log in -- you should land on the Jobs page (not the old "Welcome to AutoApply" placeholder)
3. Verify the Jobs page shows an empty state message
4. Ensure you have job preferences set (go to Profile if needed)
5. Click "Discover Jobs" button
6. Verify the status bar appears showing "Discovering jobs..."
7. Wait for completion -- status bar should turn green with job count
8. Verify job listings appear in the table
9. Click a column header to verify sorting works
10. Click a job row to verify the side panel opens with full details
11. Verify "View Original" link opens the real job posting
12. Check that navigation shows "Jobs" link and it is active
  </action>
  <verify>Human visual and functional verification</verify>
  <done>User confirms: jobs page loads as default, scraping triggers and completes, table displays results with sorting, side panel shows details, status bar works correctly.</done>
</task>

</tasks>

<verification>
- Jobs page loads as default dashboard (no placeholder)
- Empty state shows when no jobs exist
- Discover Jobs button triggers scraping
- Status bar shows progress and auto-refreshes on completion
- Table displays jobs with sorting and pagination
- Side panel shows job details with original link
- Navigation updated with Jobs link
</verification>

<success_criteria>
- Jobs page replaces placeholder dashboard as default landing after login
- User can trigger scraping with one button click
- Status bar shows real-time scrape progress via polling
- Data table displays job listings with sortable columns: title, company, location, date, platform
- Clicking a job row opens side panel with full details and link to original posting
- Stale jobs have visual indicator
- Table auto-refreshes after scrape completion
</success_criteria>

<output>
After completion, create `.planning/phases/03-scraping-infrastructure/03-03-SUMMARY.md`
</output>
